# 测试专用：H2 内存库 + 每次重建 schema（隔离测试数据）
spring:
  datasource:
    url: jdbc:h2:mem:diffpay_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql